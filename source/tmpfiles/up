if { ln -fns /proc/self/fd /dev/fd }
if { ln -fs /proc/self/fd/0 /dev/stdin }
if { ln -fs /proc/self/fd/1 /dev/stdout }
if { ln -fs /proc/self/fd/2 /dev/stderr }
if { mkdir -p /run/lock }
if { touch /run/utmp }
if { install -d -m 1733 /tmp/.X11-unix }
